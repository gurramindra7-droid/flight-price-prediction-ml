[project]
name = "flight-price-app"
version = "0.1.0"
requires-python = ">=3.12,<3.15"
dependencies = [
    "fastapi",
    "pandas",
    "scikit-learn",
    "joblib"
]

[tool.vercel]
entrypoint = "api.predict:app"